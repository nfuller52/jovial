<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Jovial</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body class="h-full relative bg-gradient-to-br from-indigo-500 from-10% via-sky-500 via-35% to-pink-400 to-90%">
    <main class="container mx-auto py-8 px-4 flex flex-col h-full">
      <div class="text-center">
        <h1 class="mx-auto text-5xl font-extrabold leading-none tracking-tight mt-0 mb-2 text-white drop-shadow-sm">
          Jovial
        </h1>
      </div>
      <div class="flex items-center justify-center flex-grow my-8 py-8 rounded-xl backdrop-blur-xl bg-white/10">
        <%= yield %>
      </div>
      <% if GIT_BRANCH.present? %>
        <div class="text-center mx-auto inline-block px-8 py-4 rounded-xl backdrop-blur-md bg-white/10">
          <span class="text-xl text-white drop-shadow-md"><%= GIT_BRANCH %></span>
        </div>
      <% end %>
    </main>
  </body>
</html>
